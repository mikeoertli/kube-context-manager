# Command help

Generated by `python3 scripts/update-help.py` from the CLI.

## `kcm`

```text
Choose Kubernetes profiles and contexts for this shell. Source kubeconfigs are shared; context selection uses environment variables and the kcmkubectl, kcmhelm, and kcmk9s shell wrappers. Expiry resets an interactive shell to local and cancels an Enter-submitted command at a stale prompt.

Usage:
  kcm [flags]
  kcm [command]

Available Commands:
  can-i       Check a live API permission without executing the action
  clear       Clear the selected context, keeping this shell's profile
  completion  Print shell completions, including profile/context names
  context     Select a context in this profile; omit name for fzf
  contexts    List available contexts, servers, namespaces, and source files
  doctor      Validate settings, profile configs, root waivers, and fzf
  exec        Run a supported client with the shell-selected context
  help        Help about any command
  init        Print shell integration (source it in your startup file)
  install     Import a kubeconfig, with optional readable names and copy/move
  list        List discovered contexts grouped by profile, with current selections marked
  namespace   Choose a shared namespace, or create one and switch to it
  permissions Inspect live permissions in one namespace; never cached
  profile     Change this shell's profile; omit name for fzf
  profiles    List profile directories and default timeouts
  prompt      Print compact profile/context information for Starship
  renew       Explicitly restart the active context's configured timeout
  settings    Locate or initialize TOML settings
  status      Show profile, context, namespace, and expiry
  unwaive     Remove a non-local root waiver
  version     Show the development version
  waive       Allow a non-local context at the kube root
  waivers     List non-local root waivers (context, source file, server)

Flags:
  -h, --help              help for kcm
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")

Use "kcm [command] --help" for more information about a command.
```

## `kcm profile`

```text
Expose only the chosen profile's directory in the context selector. Switching profiles clears the selected context and timeout; select a context afterwards. New shells start local.

Usage:
  kcm profile [name] [flags]

Examples:
  kcm profile
  kcm profile prod
  kcm context customer-a

Flags:
  -h, --help   help for profile

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm context`

```text
Select a context for this shell without writing current-context to the source file. Use '-' to return to the previous context. Duplicate names require --file or interactive selection.

Usage:
  kcm context [name|-] [flags]

Aliases:
  context, ctx

Examples:
  kcm context
  kcm ctx kind-local
  kcm context customer-a --file ~/.kube/prod/customer.yaml

Flags:
      --file string   Disambiguate by source kubeconfig path
  -h, --help          help for context

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm namespace`

```text
Update the namespace in the selected source kubeconfig. Other shells using that same context will see the change. Without a name, list existing namespaces with fzf, including a 'Create new namespace…' choice. --create creates a namespace in the selected cluster before switching; omit its name to prompt. A name without --create only updates the config and does not contact the cluster. Failed creation or cancellation leaves the selection unchanged.

Usage:
  kcm namespace [name] [flags]

Aliases:
  namespace, ns

Examples:
  kcm ns
  kcm ns app
  kcm ns feature-demo --create
  kcm ns --create

Flags:
      --create   Create in the selected cluster, then switch (prompts for name if omitted)
  -h, --help     help for namespace

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm list`

```text
List contexts in every configured profile, including empty profiles. Show namespaces, servers, and source paths. Mark the effective global context used by standard clients with '*' and this shell's KCM selection with '>'. Global selection follows KUBECONFIG merge precedence, or ~/.kube/config when unset; saved defaults in inactive files are not marked. Reads local configs only; does not switch profiles, modify configs, run credential helpers, or query permissions.

Usage:
  kcm list [flags]

Examples:
  kcm list

Flags:
  -h, --help   help for list

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm contexts`

```text
List available contexts, servers, namespaces, and source files

Usage:
  kcm contexts [flags]

Flags:
  -h, --help    help for contexts
      --names   Print only context names

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm profiles`

```text
List profile directories and default timeouts

Usage:
  kcm profiles [flags]

Flags:
  -h, --help   help for profiles

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm status`

```text
Show profile, context, namespace, and expiry

Usage:
  kcm status [flags]

Flags:
  -h, --help   help for status

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm clear`

```text
Clear the selected context, keeping this shell's profile

Usage:
  kcm clear [flags]

Flags:
  -h, --help   help for clear

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm renew`

```text
Explicitly restart the active context's configured timeout

Usage:
  kcm renew [flags]

Flags:
  -h, --help   help for renew

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm install`

```text
Import selected contexts into a profile. Missing choices are prompted. Remote configs require --profile with --no-prompt. Existing destinations are never overwritten. Certificate/key references are embedded; token-file and exec paths are preserved as absolute paths. --move requires importing every source context.

Usage:
  kcm install <path> [flags]

Examples:
  kcm install ./download.yaml
  kcm install ./download.yaml --profile prod --rename-context customer-a --no-prompt
  kcm install ./local.yaml --no-prompt --dry-run

Flags:
      --all-contexts            Import every context into the destination profile
      --context string          Source context to import (required when ambiguous)
      --copy                    Keep source (default)
      --dry-run                 Validate and print the plan without writing
      --filename string         Destination basename (default: source basename)
  -h, --help                    help for install
      --move                    Delete source only after a complete, verified import
      --namespace string        Installed namespace (default: preserve)
      --no-prompt               Do not prompt; preserve omitted names
      --profile string          Destination profile
      --rename-cluster string   New cluster display name
      --rename-context string   New context display name
      --rename-user string      New user display name

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm waive`

```text
Allow a non-local context at the kube root. Waivers are stored in kube_root/.kcm_waivers and bind to a context name, canonical file path, and exact server address. Use --file for duplicate names.

Usage:
  kcm waive <context> [flags]

Flags:
      --file string   Disambiguate the source kubeconfig
  -h, --help          help for waive

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm waivers`

```text
List non-local root waivers (context, source file, server)

Usage:
  kcm waivers [flags]

Flags:
  -h, --help   help for waivers

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm unwaive`

```text
Remove a non-local root waiver. Waivers are stored in kube_root/.kcm_waivers and bind to a context name, canonical file path, and exact server address. Use --file for duplicate names.

Usage:
  kcm unwaive <context> [flags]

Flags:
      --file string   Disambiguate the source kubeconfig
  -h, --help          help for unwaive

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm doctor`

```text
Validate settings, profile configs, root waivers, and fzf

Usage:
  kcm doctor [flags]

Flags:
  -h, --help    help for doctor
      --quiet   Print errors only

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm init`

```text
Initialize shell-local selection and expiry hooks, plus kcmkubectl, kcmhelm, and kcmk9s functions that pass the selected context to their client. Standard client commands, aliases, and functions are preserved. Source this output in your shell startup file.

Usage:
  kcm init <zsh|bash> [flags]

Examples:
  eval "$(kcm init zsh)"
  eval "$(kcm init bash)"

Flags:
  -h, --help   help for init

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm completion`

```text
Print shell completions, including profile/context names

Usage:
  kcm completion <zsh|bash|fish|powershell> [flags]

Flags:
  -h, --help   help for completion

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm settings`

```text
Locate or initialize TOML settings

Usage:
  kcm settings [command]

Available Commands:
  init        Write the example settings; never overwrite an existing file
  path        Print the effective settings path

Flags:
  -h, --help   help for settings

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")

Use "kcm settings [command] --help" for more information about a command.
```

## `kcm settings init`

```text
Write the example settings; never overwrite an existing file

Usage:
  kcm settings init [flags]

Flags:
  -h, --help   help for init

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm settings path`

```text
Print the effective settings path

Usage:
  kcm settings path [flags]

Flags:
  -h, --help   help for path

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm prompt`

```text
Print compact profile/context information for Starship

Usage:
  kcm prompt [flags]

Flags:
  -h, --help   help for prompt

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm permissions`

```text
Query the inspected context's SelfSubjectRulesReview on demand. Omit the context to use this shell's selection. Named contexts must be in the current profile; use --file for duplicate names. Does not switch contexts or modify kubeconfigs. The compact table preserves resource-name restrictions; --details prints the full returned status as JSON. Incomplete results are labeled: missing rules then mean unknown, not denied. No cache, automatic checks, or picker queries.

Usage:
  kcm permissions [context] [flags]

Examples:
  kcm permissions
  kcm permissions customer-a -n app
  kcm permissions --details

Flags:
      --details                    Print the full returned rule status as JSON after the scope header
      --file string                Disambiguate by source kubeconfig path within this profile
  -h, --help                       help for permissions
  -n, --namespace string           Namespace (default: inspected context's namespace, or default)
      --request-timeout duration   Timeout for the live check; must be positive (default 10s)

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm can-i`

```text
Use SelfSubjectAccessReview to check one action with the inspected context's credentials. Resource names, singular names, and short names are resolved through live API discovery. Use --subresource for log, exec, or another subresource. Cluster-scoped resources are detected automatically; -A checks a namespaced action across all namespaces. Does not execute the action, switch context, or cache results. Prints yes or no: exit 0 means allowed, 1 means not allowed, 2 means the check failed or is inconclusive. An allowed result does not guarantee that admission policies or other execution requirements will permit the actual operation.

Usage:
  kcm can-i <verb> <resource[.group][/name]> [flags]

Examples:
  kcm can-i list pods -n app
  kcm can-i delete deployments.apps --context customer-a -n app
  kcm can-i get pods/my-pod --subresource log -n app
  kcm can-i list nodes
  kcm can-i list pods -A

Flags:
  -A, --all-namespaces             Check the action across all namespaces
      --context string             Inspect a context in this profile without switching
      --file string                Disambiguate by source kubeconfig path within this profile
  -h, --help                       help for can-i
  -n, --namespace string           Namespace (default: inspected context's namespace, or default)
      --request-timeout duration   Timeout for the live check; must be positive (default 10s)
      --subresource string         Check a subresource, such as log or exec

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm exec`

```text
Pass the selected context explicitly to kubectl, helm, or k9s. Useful in scripts where interactive shell functions are unavailable. This command does not enforce expiry or revoke access.

Usage:
  kcm exec -- <kubectl|helm|k9s> [args...] [flags]

Flags:
  -h, --help   help for exec

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm version`

```text
Show the development version

Usage:
  kcm version [flags]

Flags:
  -h, --help   help for version

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```

## `kcm help`

```text
Help provides help for any command in the application.
Simply type kcm help [path to command] for full details.

Usage:
  kcm help [command] [flags]

Flags:
  -h, --help   help for help

Global Flags:
      --settings string   Settings file (also KCM_SETTINGS) (default "~/.config/kcm/kcm_settings.toml")
```
